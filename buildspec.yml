version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - codepipeline-demo-west-2/codepipeline
    - appspec.yml
  discard-paths: yes